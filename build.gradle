allprojects {
    apply plugin: "idea"

    repositories {
        mavenCentral()
        jcenter()
        mavenLocal()
    }

    tasks.withType(Jar) {
        destinationDir = file("$rootDir/out/jar")
    }
}